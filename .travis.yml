sudo: required
services:
  - docker

before_install:
  - docker build -t uirmak/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run uirmak/react-test npm test -- --coverage

after_success:
  - docker build -t uirmak/multi-client ./client
  - docker build -t uirmak/multi-nginx ./nginx
  - docker build -t uirmak/multi-server ./server
  - docker build -t uirmak/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push uirmak/multi-client
  - docker push uirmak/multi-nginx
  - docker push uirmak/multi-server
  - docker push uirmak/multi-worker
